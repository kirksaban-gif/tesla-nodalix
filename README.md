# tesla-nodalix

Hosts the Tesla Fleet API developer public key at
`/.well-known/appspecific/com.tesla.3p.public-key.pem`
served over HTTPS via GitHub Pages on `tesla.nodalix.net`.

Public key only — no secrets here. Private key is stored offline.
